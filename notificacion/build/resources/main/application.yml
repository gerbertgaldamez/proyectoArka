spring:
  application:
    name: notificacion
  r2dbc:
    url: r2dbc:sqlserver://localhost:1433;database=notificaciones
    username: sa
    password: YourStrong!Passw0rd
    pool:
      initial-size: 5
      max-size: 20
  mail:
    host: smtp.example.com
    port: 587
    username: user@example.com
    password: change-me
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8090

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

logging:
  level:
    root: info
    org.springframework.r2dbc: debug
    org.springframework.web: info
    io.r2dbc.mssql: debug
